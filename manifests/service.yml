apiVersion: v1
kind: Service
metadata:
    name: springyaml
spec:
    type: LoadBalancer
    ports:
    - port: 80 
      protocol: TCP
      targetPort: 8085
    selector:
        app: springyaml
